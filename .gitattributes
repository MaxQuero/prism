# Strip notebook outputs and execution metadata before Git sees them.
# One-time setup per clone: cd services/ml-engine && uv run nbstripout --install --attributes ../../.gitattributes
*.ipynb filter=nbstripout
